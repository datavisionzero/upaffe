# MVP release acceptance

This matrix maps the six success criteria in [VISION.md, section 13](../VISION.md#13-what-makes-the-mvp-successful) to observable checks. Run them from the same clean source revision as the image and CLI candidate. A green command alone is not evidence: inspect the named state changes and keep the revision, workflow URLs, image digest, and command results in the release record. Fixture hosts and addresses below are fictional.

| Vision criterion | Reproducible check | Expected evidence |
| --- | --- | --- |
| 1. Install and operate published images without a source build | `scripts/check-production-workflow.sh ghcr.io/datavisionzero/upaffe:sha-PREVIOUS_FULL_SHA ghcr.io/datavisionzero/upaffe:sha-CURRENT_FULL_SHA`; `scripts/check-production-compose.sh`; the image jobs in [CI](../.github/workflows/ci.yml) | Empty Compose install reaches ready, one-time operator setup works, recreation keeps state, failed update rolls back, current image reports its revision, backup restores into a separate project and volume, and a future migration is rejected by the older image. The rehearsal pulls published images and does not compile the app. |
| 2. Agent-only administration and one-call report | `scripts/smoke.sh`, especially CLI project, email, HTTP, push, monitor-test, and `project report` steps; `src/cli/internal/cmd/root_test.go` | With an authorized credential, `ua` creates and changes projects and both monitor types, configures and tests email, runs a website check, and returns a project report with the failure reason, last success, next due time, and delivery state. Browser-session mutations in the same smoke test observe the same persisted facts. |
| 3. Timely incidents, alerts, and recovery | `scripts/smoke.sh` HTTP threshold, explicit push failure, missing-report deadline, SMTP fixture, and recovery steps; `HttpCheckEvaluationPersistenceTests`, `PushDeadlinePersistenceTests`, `NotificationIntentTests` | The first of two HTTP failures remains below threshold and the second opens one incident; explicit push failure opens one immediately; each expired push mode records one missing report at its interval plus tolerance. The SMTP fixture receives one alert for an eligible incident and a later recovery after fresh success, with no duplicate or obsolete alert. |
| 4. Timed maintenance | `scripts/smoke.sh` overlapping project and monitor maintenance steps; `NotificationIntentTests`; `EmailDeliveryPersistenceTests` | Checks and reports continue while notification is held. An incident resolved during maintenance stays silent; an incident still open after the final scope expires becomes eligible for one alert. The project report and browser session expose the maintenance state. |
| 5. Restart and independent progress detection | `scripts/smoke.sh` restarts; `scripts/check-production-workflow.sh` (which runs `scripts/check-production-heartbeat.sh`); `ScheduledHttpCheckPersistenceTests`, `PushDeadlinePersistenceTests`, `EmailDeliveryPersistenceTests`, `MonitoringProgressTests`, `HeartbeatTests` | Configuration, incident identity, deadlines, and pending delivery survive recreation. A long worker gap creates no invented success; failures and retries remain visible. A ready but stalled instance returns nonprogressing status, sends no heartbeat, and sends again when both workers resume, so an independent receiver can detect silence. |
| 6. Dashboard diagnosis and documented integration | `src/web/src/shell/DashboardView.test.tsx`, `src/web/src/shell/ProjectOverviewView.test.tsx`, `InstanceOverviewTests`, `ProjectReportTests`, `scripts/smoke.sh`; the [CLI guide](./cli.md), [HTTP monitor guide](./http-monitoring.md), and [push report guide](./push-monitoring.md) | The dashboard names the affected project, monitor, incident reason, overdue execution, maintenance, and delivery trouble, with links to detail. The report API and CLI expose the same safe diagnosis. A website uses the documented CLI monitor operations; a backup script uses the documented completion-report or simple report HTTP operation without an SDK. |

## Release gate procedure

1. Confirm the candidate commit is on `main` and its [CI workflow](../.github/workflows/ci.yml) passed, including the composed system test and both native image tests. Record the run URL and commit SHA.
2. Build a nonpublishing candidate with the [CLI artifact workflow](../.github/workflows/cli-artifacts.yml). Download `cli-release-bundle`, run `sha256sum --check SHA256SUMS` (or `shasum -a 256 -c SHA256SUMS`), and confirm `source-revision.txt` equals the candidate commit. Record the run URL.
3. Run the published-image production rehearsal above against the previous and candidate images. Record the two image digests and the script result; its phases must reach the restored and verified state. Never record fixture credentials, reporting URLs, or receiver URLs.
4. Run `scripts/check.sh` on a clean candidate revision. Keep the test counts and any failure as a release blocker. Check every matrix row against its expected evidence; file and resolve any unexplained deviation before sign-off.

The rehearsal and CLI candidate use local fixtures and generated credentials. They do not prove public DNS, real inbox delivery, or operation of an external heartbeat receiver; operators still verify those site-specific integrations during installation.

## Initial candidate record (2026-09-19)

- Source revision `0623fd0d8cba41f267dcbff3761b09a18c95368d`: [main CI](https://github.com/datavisionzero/upaffe/actions/runs/35471088410) passed all jobs, including the composed system test, both image architectures, and revision image publication. The revision image index digest is `sha256:fd822c1b92581b2bab1748909d5b32f94d241f863299333978287480b098d7f0`; inspection found Linux amd64 and arm64 manifests.
- [Nonpublishing CLI candidate](https://github.com/datavisionzero/upaffe/actions/runs/35471099815) passed four native builds and archive executions. The downloaded bundle had four matching checksums and `source-revision.txt` equaled the source revision above.
- `scripts/check.sh` passed from clean acceptance-document revision `27b88a7`: 103 unit, 135 PostgreSQL integration, and 33 web tests, plus CLI tests and vet, builds, and Compose checks. This revision changed documentation only after the source revision above.
- `scripts/check-production-workflow.sh` passed with previous published image `sha-b7eae47af4bd77451c631c6428db6af03402161a` (index digest `sha256:a6bdc71817eb5f290bccc8a25a4bed836d086e9e016020561401e2fc3c9a230f`) and current published image `sha-0623fd0d8cba41f267dcbff3761b09a18c95368d`. It observed empty bootstrap, persisted identity and work after recreation, a failed-pull rollback, successful update, rejection of an incompatible future schema by the older image, and isolated backup restore. Its HTTPS fixture observed sends while healthy, no sends while workers were stalled despite live and ready HTTP, and sends after progress resumed.
- An earlier main CI smoke attempt on the previous image revision returned one unlocated HTTP 500 after an application restart. Its retry, the later PR smoke test, the current main CI smoke test, and local smoke runs passed. Treat any recurrence on the final candidate as a release blocker and capture the failing operation before sign-off.

This record establishes the matrix before release automation and final documentation land. Repeat the gate against the final candidate commit; a passing earlier revision does not sign off a later one.
